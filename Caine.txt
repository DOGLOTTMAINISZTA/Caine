<!DOCTYPE html>
<html lang="hu">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Amazing Digital Space</title>
    <style>
        /* Szigorúan minden stílus maradt, csak az img kapott újat */
        body, html {
            margin: 0;
            padding: 0;
            scroll-behavior: smooth;
            font-family: 'Courier New', Courier, monospace;
            background-color: #050505;
            color: #ffffff;
            overflow-x: hidden;
        }

        section {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
        }

        h1 {
            color: #ff0000;
            text-shadow: 3px 3px #3498db;
            font-size: 3rem;
            text-transform: uppercase;
        }

        .intro-text {
            max-width: 600px;
            font-size: 1.2rem;
            margin-bottom: 30px;
            color: #ecf0f1;
        }

        .scroll-btn {
            background: #ff0000;
            color: white;
            border: none;
            padding: 15px 40px;
            font-size: 1.1rem;
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            border-radius: 50px;
            box-shadow: 0 5px 15px rgba(255, 0, 0, 0.4);
            transition: transform 0.2s, background 0.3s;
        }

        .scroll-btn:hover {
            transform: scale(1.1);
            background: #c0392b;
        }

        .yt-box {
            background: #1a1a1a;
            padding: 40px;
            border-radius: 20px;
            border: 2px solid #3498db;
        }

        .yt-link {
            color: #ff0000;
            text-decoration: none;
            font-size: 1.5rem;
            font-weight: bold;
        }

        /* --- EZ AZ ÚJ KÉP STÍLUS --- */
        .caine-image-container {
            width: 300px;
            height: 300px;
            margin: 20px auto;
            position: relative;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        /* Ez a technikai csoda integrálja a plüsst */
        .caine-plush {
            position: absolute;
            width: 100%;
            height: 100%;
            background-repeat: no-repeat;
            background-position: center;
            background-size: contain;
            